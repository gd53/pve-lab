# Node Configuration
# Copy this file to node.env and fill in your values.
# For multi-node setups, copy to node-<name>.env instead.
# Placeholders only — no real IPs or secrets.

NODE_NAME="pve"
NODE_IP="192.168.x.x"
GATEWAY_IP="192.168.x.1"
DNS="192.168.x.1"
DOMAIN="local"
