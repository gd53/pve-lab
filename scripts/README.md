# scripts/

Shell scripts for homelab automation. Run in sequence per [reference/sequence-single.md](../reference/sequence-single.md).

Each script has a matching doc in `docs/` that explains what it does.
